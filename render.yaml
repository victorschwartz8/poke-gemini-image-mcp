services:
  - type: web
    name: poke-gemini-image-mcp
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python src/server.py
    envVars:
      - key: GEMINI_API_KEY
        sync: false
